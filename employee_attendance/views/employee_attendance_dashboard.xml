<odoo>
    <record id="view_employee_attendance_kanban" model="ir.ui.view">
        <field name="name">employee.attendance.kanban</field>
        <field name="model">employee.attendance</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_example" default_group_by="attendance_status">
                <field name="employee_id"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <strong><field name="employee_id"/></strong>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="employee_attendance_dashboard_action" model="ir.actions.act_window">
        <field name="name">Dashboard Attendance</field>
        <field name="res_model">employee.attendance</field>
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="view_employee_attendance_kanban" />
        <field name="context">{"create": False}</field>
    </record>
</odoo>